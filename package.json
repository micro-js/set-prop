{
  "name": "@micro-js/set-prop",
  "description": "Non-destructively set a nested property, given its path",
  "repository": "git://github.com/micro-js/set-prop.git",
  "version": "0.1.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@micro-js/clone-shallow": "^0.1.0",
    "@micro-js/is-function": "^1.0.4"
  },
  "devDependencies": {
    "tape": "^4.2.2",
    "standard": "^5.4.1"
  }
}
